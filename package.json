{
  "name": "sass-minimal",
  "version": "1.0.0",
  "description": "A minimal sass compiler configuration.",
  "main": "index.js",
  "devDependencies": {
    "http-server": "^14.1.1",
    "sass": "^1.49.7"
  },
  "scripts": {
    "dev": "http-server & sass --watch --update --style=expanded assets/scss:assets/css",
    "build": "sass --no-source-map --style=compressed assets/scss:assets/css",
    "serve": "http-server"
  },
  "author": "Sam Glover",
  "license": "UNLICENSED",
  "dependencies": {
    "sass": "^1.49.7"
  }
}
